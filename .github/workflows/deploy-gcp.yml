name: Deploy Backend to Google Cloud Run

on:
  push:
    branches:
      - dev_fabio  # Dispara em qualquer push na branch dev_fabio

env:
  PROJECT_ID: epatrimonio-509317       # ID do seu projeto no GCP
  SERVICE_NAME: epatrimonio-backend   # Nome do serviço no Cloud Run
  REGION: us-central1                  # Região de sua preferência

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Código
      uses: actions/checkout@v4

    - name: Autenticar no Google Cloud
      uses: google-github-actions/auth@v2
      with:
        credentials_json: ${{ secrets.GCP_SA_KEY }}

    - name: Configurar Google Cloud SDK
      uses: google-github-actions/setup-gcloud@v2

    - name: Build e Push da Imagem usando Cloud Build
      # O Dockerfile está em backend/api/, então o contexto deve apontar para lá
      run: |
        gcloud builds submit --tag gcr.io/${{ env.PROJECT_ID }}/${{ env.SERVICE_NAME }}:${{ github.sha }} ./backend/api

    - name: Implantar no Cloud Run
      run: |
        gcloud run deploy ${{ env.SERVICE_NAME }} \
          --image gcr.io/${{ env.PROJECT_ID }}/${{ env.SERVICE_NAME }}:${{ github.sha }} \
          --platform managed \
          --region ${{ env.REGION }} \
          --allow-unauthenticated \
          --port 8080
